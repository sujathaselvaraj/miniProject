import { ConvertionPipe } from './convertion.pipe';

describe('ConvertionPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertionPipe();
    expect(pipe).toBeTruthy();
  });
});
